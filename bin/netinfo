#!/bin/bash

printf "#--# sudo ifconfig -a\n"
sudo ifconfig -a

printf "#--# netstat -rn\n"
netstat -rn

printf "#--# netstat -ni\n"
netstat -ni

printf "#--# netstat -ntlp\n"
netstat -ntlp

printf "#--# netstat -nul\n"
netstat -nul
